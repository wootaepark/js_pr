// 숫자가 아닌 값을 숫자 타입으로 변환
console.log(Number('0'));

// 만약 Number의 매개변수로 숫자가 아닌 값을 넣으면 NaN이 반환된다.
Number('-1');
Number('10.55');

+'0';
+'-1';
+'10.55';

+true;
+false;

'0'*1; //0
'-1'*1;
'10.55'*1;

true*1; // 1
false*1; //0