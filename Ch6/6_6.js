var string;
string = '문자열'; 
string = "문자열";
string = `문자열`;
string = '작은따옴표로 감싼 문자열 내의 "큰따옴표"는 문자열로 인식된다.';
string = "큰따옴표로 감싼 문자열 내의 '작은따옴표'는 문자열로 인식된다."

console.log(string);

// 모두 같은 문자열의 의미 (보통은 작은 따옴표, 필요한 경우 백틱을 쓴다.)